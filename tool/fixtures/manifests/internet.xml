<?xml version="1.0" encoding="utf-8"?>
<!-- A permission nobody asked for. The gate asserts the WHOLE set, not just
     the absence of two names: this app's premise is that it has no network
     path, and INTERNET arriving through a transitive library is exactly the
     way that promise would be broken without anybody editing a file. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.applander.nearlystop">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application android:label="NearlyStop">
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver" />
    </application>
</manifest>
